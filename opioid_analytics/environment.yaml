# conda env create -f environment.yaml

name: opioid_env

channels:
  - defaults
  - conda-forge

dependencies:

# Standard packages for running in VS Code
  - python
  - pip
  - jupyter

# Data manipulation
  - numpy
  - pandas
# Faster excel readers
  - openpyxl
  - python-calamine
  - pyarrow

# Data Plotting
  - matplotlib
  - seaborn
  - plotly

# Data Analysis and Model Buliding
  - scikit-learn
  - scipy
  - statsmodels # May not work with due to OpenSSL error; Use pip

  # Which one do you want?
  # - pytorch
  # - tensorflow

# Documentation
  - sphinx


  - pip:
    # Pip items here
    - statsmodels